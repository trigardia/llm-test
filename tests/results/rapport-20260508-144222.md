# Rapport de tests — Modèles LLM
**Date** : 2026-05-08 14:42:22
**Machine** : MacBook Pro M5 Max — 128 Go RAM
**Mode** : Un modèle chargé à la fois — marge 20 Go

---
## nomic-embed-text

**Taille** : ~0 Go | **Date** : 2026-05-08 14:42:22

**Prompt** : `LLM local sécurisé sur Apple Silicon M5 Max`


**Résultat** : Vecteur 768 dimensions — min=-3.7386 max=2.6622 | **Durée** : 0s | **Statut** : ✅ OK


---
## llama3.1:8b

**Taille** : ~5 Go | **Date** : 2026-05-08 14:42:25

**Prompt** : `En une phrase : quelle est la première règle OWASP Top 10 pour les API ?`


**Réponse** :
```
La première règle de OWASP Top 10 pour les API est d&#39;utiliser des secre[5D[K
secrets et des identifiants appropriés lors du développement et de l&#39;ex[8D[K
l&#39;exécution des applications. Cela inclut notamment le stockage sécuris[7D[K
sécurisé des clés d’API, le cryptage des données sensibles et la validation[10D[K
validation efficace des entrées de données provenant de sources externes.
```

**Durée** : 18s | **Statut** : ✅ OK


---
## phi4:14b

**Taille** : ~9 Go | **Date** : 2026-05-08 14:42:46

**Prompt** : `Cite 3 principes SOLID appliqués à une API REST Java. Réponse courte.`


**Réponse** :
```
Bien sûr ! Voici trois des cinq principes SOLID appliqués à une API REST en[2D[K
en Java :

1. **Principe de Responsabilité Unique (Single Responsibility Principle - S[1D[K
SRP)** :
   Chaque classe ou méthode doit avoir une seule responsabilité. Dans le co[2D[K
contexte d'une API REST, cela pourrait signifier que chaque contrôleur gère[4D[K
gère un ensemble spécifique de requêtes HTTP pour une ressource donnée. Par[3D[K
Par exemple, un `UserController` est responsable des opérations CRUD (Créer[6D[K
(Créer, Lire, Mettre à jour, Supprimer) concernant les utilisateurs.

2. **Principe d'Inversion des Dépendances (Dependency Inversion Principle -[1D[K
- DIP)** :
   Les dépendances doivent être dirigées vers des abstractions plutôt que v[1D[K
vers des implémentations concrètes. Dans une API REST Java, cela se traduit[7D[K
traduit souvent par l'utilisation de Spring pour injecter des dépendances v[1D[K
via des interfaces ou des abstractions (comme les `@Service` et `@Repositor[11D[K
`@Repository`) au lieu de classes concrètes directement.

3. **Principe d'ouverture/fermeture (Open/Closed Principle - OCP)** :
   Les entités doivent être ouvertes à l'extension mais fermées aux modific[7D[K
modifications. Une API REST peut appliquer ce principe en utilisant des int[3D[K
interfaces et des classes abstraites pour permettre l'ajout de nouvelles fo[2D[K
fonctionnalités sans modifier le code existant, par exemple en ajoutant de [K
nouveaux contrôleurs pour gérer de nouvelles ressources ou en étendant les [K
services existants via des implémentations alternatives.

Ces principes aident à concevoir une API REST flexible, maintenable et évol[4D[K
évolutive.
```

**Durée** : 8s | **Statut** : ✅ OK


---
## codestral:22b

**Taille** : ~14 Go | **Date** : 2026-05-08 14:42:57

**Prompt** : `Écris une fonction PHP qui sanitise une entrée utilisateur contre les injections SQL. Réponse courte.`


**Réponse** :
```
 En PHP, la méthode à utiliser pour se protéger des injections SQL est l'éc[4D[K
l'échappement de caractères spéciaux dans les requêtes SQL. La classe PDO ([1D[K
(PHP Data Objects) permet cela en utilisant ses fonctions préparées. Voici [K
un exemple d'une fonction qui sanitise une entrée :

```php
<?php
function sanitizeInput($input, $dbConnection) {
    $stmt = $dbConnection->prepare("SELECT * FROM table WHERE column = ?");[4D[K
?");
    $stmt->bindParam(1, $input);
    $stmt->execute();
    return $stmt->fetchAll();
}
?>
```
Dans cet exemple, `$input` est l'entrée utilisateur et `$dbConnection` est [K
la connexion à votre base de données. La fonction prépare une requête SQL a[1D[K
avec un paramètre marqué par un point d'interrogation (?). Elle utilise ens[3D[K
ensuite la méthode bindParam() pour lier l'entrée utilisateur au paramètre,[10D[K
paramètre, ce qui fait que la valeur est correctement échappée et sécurisée[9D[K
sécurisée contre les injections SQL.
```

**Durée** : 10s | **Statut** : ✅ OK


---
## devstral:24b

**Taille** : ~15 Go | **Date** : 2026-05-08 14:43:10

**Prompt** : `Donne la structure Clean Architecture pour un projet Symfony avec un use case CreateUser. Réponse courte.`


**Réponse** :
```
Pour implémenter une architecture propre (Clean Architecture) dans un proje[5D[K
projet Symfony avec un use case "CreateUser", voici la structure de base :

1. **Entities** : Contient les entités principales du domaine.
   - `src/Entity/User.php`

2. **Use Cases (Application Layer)** : Contient les cas d'utilisation spéci[5D[K
spécifiques.
   - `src/Application/UseCase/CreateUser/CreateUserCommand.php`
   - `src/Application/UseCase/CreateUser/CreateUserHandler.php`

3. **Services** : Services de domaine ou services externes.
   - `src/Domain/Service/UserService.php` (si nécessaire)

4. **Repositories** : Interfaces pour les repositories.
   - `src/Domain/Repository/UserRepositoryInterface.php`

5. **Infrastructure (Persistence Layer)** : Implémentation des repositories[12D[K
repositories.
   - `src/Infrastructure/Persistence/Repository/UserRepository.php`

6. **Controllers** : Contrôleurs Symfony pour gérer les requêtes HTTP.
   - `src/Controller/UserController.php`

Exemple de structure de fichiers :

```
src/
├── Application/
│   └── UseCase/
│       └── CreateUser/
│           ├── CreateUserCommand.php
│           └── CreateUserHandler.php
├── Controller/
│   └── UserController.php
├── Domain/
│   ├── Repository/
│   │   └── UserRepositoryInterface.php
│   └── Service/
│       └── UserService.php
├── Entity/
│   └── User.php
└── Infrastructure/
    └── Persistence/
        └── Repository/
            └── UserRepository.php
```

Cela permet de séparer clairement les préoccupations et d'appliquer les pri[3D[K
principes de Clean Architecture dans un projet Symfony.
```

**Durée** : 18s | **Statut** : ✅ OK


---
## gemma3:27b

**Taille** : ~18 Go | **Date** : 2026-05-08 14:43:31

**Prompt** : `Donne une requête KQL Kibana pour détecter des tentatives brute-force SSH dans les logs ELK.`


**Réponse** :
```
```kql
source=*ssh* AND (
  (event.dataset == "ssh" AND event.action == "authentication_failure") OR
  (message:* "Failed password" OR message:* "Invalid user" OR message:* "Au[3D[K
"Authentication failure")
)
| stats count() by user, src_ip, dest_ip, timestamp
| where count() > 5 
| sort -count
```

**Explication:**

* **`source=*ssh*`**:  Filtre les logs qui contiennent le terme "ssh". Cela[4D[K
Cela aide à se concentrer sur les logs potentiellement liés à SSH. Ajustez [K
cette source si vos logs SSH sont enregistrés avec une source différente.
* **`(event.dataset == "ssh" AND event.action == "authentication_failure")`[26D[K
"authentication_failure")`**: Cette partie de la requête recherche les évén[4D[K
événements spécifiques de logs où le dataset est "ssh" et l'action est "aut[4D[K
"authentication_failure". C'est la méthode la plus précise si vos logs stru[4D[K
structurés contiennent ces champs.
* **`OR (message:* "Failed password" OR message:* "Invalid user" OR message[7D[K
message:* "Authentication failure")`**:  Si vos logs ne sont pas structurés[10D[K
structurés, cette partie recherche des phrases courantes dans les messages [K
de log qui indiquent une tentative d'authentification échouée.  Adaptez ces[3D[K
ces phrases à votre configuration SSH et à la langue de vos logs.
* **`| stats count() by user, src_ip, dest_ip, timestamp`**:  Cette partie [K
regroupe les événements en comptant le nombre de tentatives d'authentificat[15D[K
d'authentification échouées pour chaque combinaison unique de nom d'utilisa[9D[K
d'utilisateur (`user`), adresse IP source (`src_ip`), adresse IP de destina[7D[K
destination (`dest_ip`), et horodatage (`timestamp`).
* **`| where count() > 5`**:  Cette clause filtre les résultats pour ne con[3D[K
conserver que les combinaisons qui ont un nombre de tentatives d'authentifi[12D[K
d'authentification échouées supérieur à 5 (vous pouvez ajuster cette valeur[6D[K
valeur en fonction de votre seuil de tolérance). C'est le seuil qui permet [K
de détecter les attaques brute-force.
* **`| sort -count`**:  Trie les résultats par le nombre de tentatives d'au[4D[K
d'authentification échouées en ordre décroissant, afin que les attaques les[3D[K
les plus importantes apparaissent en premier.

**Conseils et améliorations:**

* **Ajuster le seuil:**  La valeur `5` dans `where count() > 5` est un seui[4D[K
seuil simple.  Adaptez-le en fonction du volume normal de tentatives de con[3D[K
connexion sur votre serveur.  Un seuil trop bas générera de faux positifs, [K
tandis qu'un seuil trop élevé pourrait manquer des attaques.
* **Période de temps (Time Window):**  Pour être plus précis, envisagez d'u[3D[K
d'utiliser une fenêtre temporelle.  Par exemple, vous pourriez vouloir déte[4D[K
détecter plus de 5 tentatives échouées *par minute* ou *par heure*.  Vous p[1D[K
pouvez utiliser les fonctions `bin()` ou `datehistogram()` dans Kibana pour[4D[K
pour cela.  Voici un exemple avec `bin()`:

```kql
source=*ssh* AND (
  (event.dataset == "ssh" AND event.action == "authentication_failure") OR
  (message:* "Failed password" OR message:* "Invalid user" OR message:* "Au[3D[K
"Authentication failure")
)
| bin wtime=1m
| stats count() by user, src_ip, dest_ip, bin
| where count() > 5
| sort -count
```

*   **`wtime=1m`**:  Crée des buckets d'une minute.  Cela regroupe les évén[4D[K
événements dans des fenêtres d'une minute.
*   **`bin`**:  Ajoute un champ `bin` représentant la fenêtre temporelle.
*   Adaptez la fenêtre temporelle (`1m`, `5m`, `1h`, etc.) à vos besoins.

* **Blocage automatique (Automated Blocking):**  Si vous utilisez un systèm[6D[K
système de gestion des logs qui le permet (comme Elasticsearch Security), v[1D[K
vous pouvez configurer des règles pour bloquer automatiquement les adresses[8D[K
adresses IP sources qui dépassent un certain seuil de tentatives échouées.
* **Whitelist:**  Mettez en place une liste blanche (whitelist) pour exclur[6D[K
exclure les adresses IP légitimes qui pourraient déclencher de faux positif[7D[K
positifs (par exemple, les adresses IP de vos administrateurs).
* **Corrélation avec d'autres logs:**  Corrélez ces événements avec d'autre[7D[K
d'autres logs de sécurité (pare-feu, IDS/IPS) pour avoir une vue plus compl[5D[K
complète de l'activité malveillante.
* **Utilisation de champs structurés:**  Si possible, assurez-vous que vos [K
logs SSH sont structurés avec des champs tels que `user`, `src_ip`, `dest_i[7D[K
`dest_ip`, `timestamp`, `event.action`, `event.dataset`.  Cela rendra vos r[1D[K
requêtes KQL plus précises et plus efficaces.
* **Analyse des motifs d'attaque:** Analysez les motifs d'attaque pour iden[4D[K
identifier les attaques les plus courantes et ajuster vos règles de sécurit[7D[K
sécurité en conséquence.

**Test:**

Testez soigneusement votre requête KQL sur une période de temps limitée ava[3D[K
avant de la déployer en production.  Assurez-vous qu'elle ne génère pas de [K
faux positifs et qu'elle détecte bien les attaques brute-force.
```

**Durée** : 46s | **Statut** : ✅ OK


---
## llama3.3:70b

**Taille** : ~43 Go | **Date** : 2026-05-08 14:44:20

> ✗ RAM insuffisante : 48 Go dispo, 63 Go requis


---
## qwen2.5-coder:32b (sandbox)

**Port** : 11435 | **Date** : 2026-05-08 14:44:23

**Prompt** : `Écris une fonction Python qui parse un fichier YAML de config Ansible. Réponse courte.`


**Statut** : ❌ ÉCHEC | **Durée** : 3s


---
*Rapport généré automatiquement par test-models.sh*
